How to install properly ft_package:

python3 setup.py sdist bdist_wheel

pip install ./dist/ft_package-0.0.1.tar.gz

After this, a test program can be run using ft_package and count_in_list function.

to uninstall the package just use:

python clean.py
or : pip uninstall ft_package